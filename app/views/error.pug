extends layout

block content
  .container
    .row
      .col-md-12
        .alert.alert-danger
          h1= message
          h2= error.status
        if error.stack && process.env.NODE_ENV !== 'production'
          pre.mt-4.p-3.bg-light.rounded #{error.stack} 